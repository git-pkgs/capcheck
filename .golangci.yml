version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - unparam
    - goconst
  settings:
    goconst:
      ignore-tests: true
